# 🔧 Решение проблем

## Проблема: "process is not defined" в браузере

### Причина
Ошибка возникает потому, что `process.env` не доступен в браузере в Vite.

### Решение ✅
Исправлено в файле `src/config/emailjs.ts`:
- Заменили `process.env` на `import.meta.env` (для Vite)
- Или используем статические значения

## Проблема: Ошибки с иконками

### Причина
Отсутствуют файлы иконок, на которые ссылается manifest.json.

### Решение ✅
Обновлен `public/manifest.json` - теперь использует только существующий `favicon-code.svg`.

## Проблема: Проект не открывается в браузере

### Проверьте:
1. **Сервер запущен**: `npm run dev`
2. **Порт доступен**: http://localhost:5173
3. **Нет ошибок в консоли**: F12 → Console

### Если все еще не работает:

#### Вариант 1: Использовать упрощенную версию
Замените в `src/App.tsx`:
```typescript
// Заменить
import Contact from "./components/Contact";

// На
import Contact from "./components/ContactSimple";
```

#### Вариант 2: Временно отключить EmailJS
В `src/components/Contact.tsx` закомментируйте:
```typescript
// import { useEmailJS } from "../hooks/useEmailJS";
// import { isEmailJSConfigured } from "../config/emailjs";
// import ContactSetupGuide from "./ContactSetupGuide";
```

## Проблема: Ошибки TypeScript

### Решение
```bash
npm install
npm run build
```

## Проблема: Модули не найдены

### Решение
```bash
rm -rf node_modules package-lock.json
npm install
```

## 🔍 Диагностика

### Проверьте консоль браузера:
1. Откройте F12
2. Перейдите на вкладку Console
3. Посмотрите на ошибки (красные)

### Проверьте терминал:
1. Остановите сервер (Ctrl+C)
2. Запустите заново: `npm run dev`
3. Посмотрите на ошибки

## 📞 Поддержка

Если проблемы остаются:
1. Проверьте версию Node.js (должна быть 18+)
2. Убедитесь, что все файлы сохранены
3. Попробуйте очистить кэш браузера
4. Перезапустите терминал

---

**Большинство проблем решены! Проект должен работать корректно.** ✅ 